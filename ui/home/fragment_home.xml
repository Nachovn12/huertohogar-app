<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F7F8FA">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <!-- Botones de carga con estilo profesional -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginBottom="24dp">

            <androidx.cardview.widget.CardView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginEnd="12dp"
                android:elevation="6dp"
                app:cardCornerRadius="16dp"
                app:cardBackgroundColor="#FFFFFF">

                <Button
                    android:id="@+id/btnApi"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Cargar desde Rest API"
                    android:textSize="18sp"
                    android:textColor="#2196F3"
                    android:drawableLeft="@android:drawable/ic_menu_send"
                    android:drawablePadding="8dp"
                    android:padding="16dp"
                    android:background="@drawable/bg_button_api"/>
            </androidx.cardview.widget.CardView>

            <androidx.cardview.widget.CardView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="12dp"
                android:elevation="6dp"
                app:cardCornerRadius="16dp"
                app:cardBackgroundColor="#FFFFFF">

                <Button
                    android:id="@+id/btnDb"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Cargar desde BD Local"
                    android:textSize="18sp"
                    android:textColor="#4CAF50"
                    android:drawableLeft="@android:drawable/ic_menu_save"
                    android:drawablePadding="8dp"
                    android:padding="16dp"
                    android:background="@drawable/bg_button_db"/>
            </androidx.cardview.widget.CardView>
        </LinearLayout>

        <!-- ...existing code... -->
        <!-- Aquí iría el resto del layout visual del HomeScreen: cabecera, ofertas, categorías, productos, etc. -->
        <!-- ...existing code... -->
    </LinearLayout>
</ScrollView>
