package com.huertohogar.huertohogar_app.ui.navigation

import androidx.compose.runtime.Composable
import androidx.navigation.NavHostController
import com.huertohogar.huertohogar_app.ui.listado.ProductViewModel

@Composable
fun AppNavGraph(
    navController: NavHostController,
    productViewModel: ProductViewModel
) {
    // Mantener solo este archivo como fuente de rutas y navegación.
    // Elimina navigation/AppNavGraph.kt si existe duplicado.

    // Implementación mínima
}
